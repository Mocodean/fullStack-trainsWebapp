<nav class="navbar navbar-expand bg-body-tertiary rounded shadow">
    <div class="container-fluid">
        <div class="navbar-brand align-items-center justify-content-center">
            <a class="nav-link" href="/">Train routes</a>
        </div>
        <div class="d-flex justify-content-between w-100">
            <ul class="navbar-nav me-auto">
                <% if (user.rang && user.rang==='admin' ) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/trains/insert">Modifine trains</a>
                    </li>
                    <% } %>
                        <% if (user.rang) { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/reservations/felhasznalo">Reservations</a>
                            </li>
                            <% } %>
            </ul>
            <ul class="navbar-nav ms-auto justify-content-center align-items-center">
                <% if (user.rang) { %>
                    <li class="nav-item">
                        <span class="navbar-text me-2">Welcome, <%= user.username %></span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Log Out</a>
                    </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link me-2" href="/login">Sign In</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/regisztracio">Sign Up</a>
                        </li>
                        <% } %>
            </ul>
        </div>
    </div>
</nav>